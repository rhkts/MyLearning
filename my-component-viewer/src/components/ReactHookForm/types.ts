export type ReactHookHormSampleInputsTypes = {
  name: string;
  gender: string;
  mail: string;
  tell: string;
  mode: "view" | "edit";
};

export enum GenderEnum {
  female = "female",
  male = "male",
  other = "other",
}
